# 编译优化文档

此处暂时记录一些优化时的算法和思路

## CFG

梳理一下定义：

+ 支配：到A的所有路径都经过B，B支配A
+ 严格支配：A支配B且A不等于B
+ 直接支配：A严格支配B且A不严格支配任何严格支配B的块，也就是离B最近的块

cfg中：

+ foreMap：每个块的前驱块的集合
+ subMap：后继块的集合
+ domiMap：记录每个块支配了哪些块
+ domiParent：记录了A块被哪个块直接支配（只有一个）
+ domiChildren：记录了A块直接支配了哪些块